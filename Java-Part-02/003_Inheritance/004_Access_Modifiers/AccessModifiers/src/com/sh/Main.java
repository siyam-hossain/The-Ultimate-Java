package com.sh;

public class Main {
    public static void main(String[] args) {
        var textBox = new TextBox();

    /*
        * Private (private)
            * Accessible only within the same class.
            * Cannot be accessed by other classes, even in the same package.
            * Provides the highest level of data hiding.
            * Commonly used for class fields (variables).

        * Default (No Access Modifier / Package-Private)
            * Accessible only within the same package.
            * Not accessible from classes in other packages.
            * Used when no access modifier is specified.

        * Protected (protected)
            * Accessible within the same package.
            * Also, accessible by subclasses, even if they are in different packages.
            * Commonly used when inheritance is intended.

        * Public (public)
            * Accessible from anywhere in the program.
            * Can be accessed by any class in any package.
            * Provides the widest level of accessibility.
     */

    }
}
